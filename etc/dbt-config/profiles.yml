# This file is managed by Meltano
# Do not edit this file manually

config:
  send_anonymous_usage_stats: False
  use_colors: True
spark-local:
  target: spark
  outputs:
    spark:
      # method: http
      method: thrift
      type: spark
      schema: default
      # schema: analytics
      # host: spark
      host: localhost
      # host: 127.0.0.1
      port: 10000
      # user: hadoop
      connect_retries: 5
      connect_timeout: 60
